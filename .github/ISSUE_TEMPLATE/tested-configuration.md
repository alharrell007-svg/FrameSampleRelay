---
name: Tested configuration
about: Report an exact successful or failing model configuration
title: "[Configuration] model + quantization + OS"
---

Do not include tokens, personal paths, personal media, or conversation history. Replace private filenames with neutral fixture names.

- Result: successful / intermittent / failing
- Tool version and archive SHA-256:
- OS and build:
- LM Studio version and inference engine:
- Node, FFmpeg, ffprobe versions:
- Chat model: exact API ID, source repository, filename, revision and quantization:
- Vision model: exact API ID, source repository, filename, projector, revision and quantization:
- Context lengths, thinking settings, GPU offload, concurrency:
- Tool-selection/system-prompt settings:
- Video container, codec, resolution, frame rate, duration, audio:
- Sample count and tool limits:
- Test mode: orchestration-only / vision-only / live end-to-end / LM Studio UI
- Attempts / correct tool invocations / completed vision analyses:
- Final-answer fidelity and obvious factual errors:
- Manual intervention and errors, including failed attempts:
- Reproduction steps using synthetic or consented nonpersonal footage:
- Sanitized doctor output and relevant final responses:
- What remains untested:
